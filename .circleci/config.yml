version: 2.1

orbs:
  common: ciclismurban/common@0.2.3

workflows:
  build-deploy:
    jobs:
      - common/build
      - common/deploy-master:
          requires:
            - common/build
          filters:
            branches:
              only: master
