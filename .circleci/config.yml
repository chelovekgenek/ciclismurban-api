version: 2.1

orbs:
  common: ciclismurban/common@0.0.1

workflows:
  build-deploy:
    jobs:
      - common/build:
          filters:
            branches:
              only:
                - master
      - common/deploy-master:
          requires:
            - common/build
          filters:
            branches:
              only: master
